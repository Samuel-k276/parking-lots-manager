#include <stdio.h>
#include structures.h

int main() {
    Parque AllParks;
    Carro AllCars;

    DataEHora ultimoRegisto;
    ultimoResgisto = inicializaPrimeiroRegisto();
    
    char input;
    while (input = getchar()) {

        switch(input) {
            case 'q':
                break;
            case 'p':
                comandoP();


        }


    }

    cleanMemory();


    return 0;
}
